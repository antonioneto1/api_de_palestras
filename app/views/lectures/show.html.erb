<p style="color: green"><%= notice %></p>

<%= render @lecture %>

<div>
  <%= link_to "Edit", edit_lecture_path(@lecture), class: "btn btn-primary" %> |
  <%= link_to "Back", lectures_path, class: "btn btn-secondary" %>

  <%= button_to "Destroy", @lecture, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
</div>

